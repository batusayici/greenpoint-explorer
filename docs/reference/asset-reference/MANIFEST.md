# Asset Reference — Organized Index (Phase 7.0)

Reviewed and sorted 56 photos (IMG_0682–IMG_0752) into component + material folders.
Originals preserved (no rotation applied — orientation left as-is). Many photos serve
several components, so they appear in more than one folder.

## Component folders

- **cornice/** (10) — best isolated close-ups: `0702`, `0703`, `0722`, `0723` (bracketed +
  dentil cornices, multiple paint colors). `0682`/`0697`/`0744` are corner cornices in context.
- **window/** (7) — **`0751` is the standout**: a single window with carved wood pilaster
  surround, near-isolated. `0729` = white scrolled stone lintels. `0704`/`0730` = oriel/bay
  windows (green cast-iron + wood). `0702` = arched window + keystone.
- **door:stoop/** (10) — **`0752` is the standout**: carved wood double door + terracotta
  surround, near-isolated. `0735` = arched black door on blue clapboard. `0746` = red door +
  canopy. `0717`/`0734` = classic row-house stoops.
- **awning/** (8) — mostly **residential entry canopies** (`0710` wood, `0736` bracketed,
  `0737` stained-glass, `0746`/`0750` peaked). `0730` = striped storefront awning (the only
  true storefront fabric awning). `0719`/`0720` = simple residential.
- **storefront/** (8) — all are **context/streetscape distance** (`0696`, `0701`, `0726`,
  `0730` rows; `0704` salon; `0748` glass). No tight bulkhead/transom close-up.
- **bay-frame/** (3) — `0704`, `0733`, `0748` (closest storefront-structure shots).
- **weathering/** (6) — strong set: `0705`/`0706` (stucco spalled to exposed brick),
  `0716` (whitewash residue + granite patch), `0738`/`0739`/`0749` (peeling paint on brick).

## Material folders (facade material/)

- **brick/** (24) — red + buff/yellow Greenpoint brick, plus clean texture swatches
  `0707` (grey roman), `0714` (buff), `0715` (red), `0716` (weathered).
- **masonry/** (8) — painted/stucco masonry: `0695` mauve, `0699` oxblood, `0711` cream,
  `0705`/`0706` stucco-over-brick, `0721` whitewash.
- **clapboard (wood-frame)/** (14) — strong set across colors: `0717` tan, `0712` dark grey,
  `0734` grey, `0735` blue, `0740` sage, `0719` red board-and-batten, `0745`/`0746` shingle,
  `0750` scalloped fish-scale shingle.
- **modern flat/** (5) — standing-seam metal: `0708`/`0709` red corrugated, `0748` red
  standing-seam, `0720` salmon, `0713` red+grey.
- **brownstone/** — **EMPTY (gap).** No clean brownstone in this set (`0752`'s surround is the
  closest terracotta/brownstone detail).
- **industrial:warehouse/** — **EMPTY (gap).** No clear industrial/warehouse facade.

## Gaps to fill (blocking parts of Phase 7.2)

1. **roll-gate/ — EMPTY.** No security roll-gate photos at all (closed slat gates + side
   housing). Needed for brick / painted-masonry / modern-flat / warehouse.
2. **Storefront / bay-frame — weak.** Only distant streetscape shots; no tight bulkhead +
   transom + pier close-up. Needed for brick / painted-masonry / modern-flat.
3. **Storefront awnings — thin.** Mostly residential entry canopies; only `0730` is a true
   storefront fabric awning.
4. **brownstone** and **industrial/warehouse** facade families have no reference.

## Rejected

- **_rejected/** — `0727`, `0728` (parked car, not architecture). `0721` kept in masonry as a
  whitewash-brick texture despite the parking sign.

## Note on cropping

Detail close-ups (cornices, windows, textures, weathering) are already tightly framed and
need no crop. The whole-facade shots could be cropped to isolate a component — flag any you
want tightened and I'll crop those specifically.
